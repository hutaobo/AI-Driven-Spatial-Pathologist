# Schemas

This folder contains exported public contracts for `spatho`.

Currently included:

- `workflow.schema.json`: formal JSON schema for workflow configuration files

You can refresh it locally with:

```bash
spatho config-schema --output /path/to/workflow.schema.json
```
